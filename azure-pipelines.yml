# Docker

trigger:
- main

resources:
- repo: self

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool: rpi
    steps:
      - bash: export $(API_TOKEN)
      - bash: make build
      - bash: make run